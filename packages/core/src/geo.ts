/**
 * Haversine great-circle distance (km). Source: the web app's
 * `lib/utils/geo.ts` (pure math). Used by the native map + "X km away"
 * distance sorting, same as the web's city-map distance chip.
 */
export { distanceKm } from "../../../lib/utils/geo";
