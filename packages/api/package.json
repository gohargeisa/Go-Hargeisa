{
  "name": "@gohargeisa/api",
  "version": "0.0.0",
  "private": true,
  "description": "Shared contract for the additive /api/v1/* read layer: request/response DTOs + a transport-injected client. Isomorphic — no next/*, no react-native, no Supabase. The website's Route Handlers import the DTOs to type their responses; the native app builds a client with createGoHargeisaApi(apiFetch).",
  "main": "src/index.ts",
  "types": "src/index.ts"
}
