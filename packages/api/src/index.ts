export * from "./types";
export * from "./client";

/** The `/api/v1` path prefix — the website mounts the routes here and the
 *  native transport prepends it. */
export const API_V1_PREFIX = "/api/v1";
